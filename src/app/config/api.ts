import {environment} from '../../environments/environment.prod';
export const baseUrl = environment.baseUrl;
export const parametersUrl = baseUrl+'/simulations';
export const resultsUrl = baseUrl+'/results';
